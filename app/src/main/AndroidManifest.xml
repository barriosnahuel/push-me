<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.github.barriosnahuel.vossosunboton"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MyCustomTheme">

        <activity
            android:name=".feature.home.HomeActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:launchMode="singleTask">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:host="@string/deeplink_home_host"
                    android:path="@string/deeplink_home_path"
                    android:scheme="@string/deeplink_app_scheme"
                />
            </intent-filter>
        </activity>

        <activity
            android:name=".feature.addbutton.AddButtonActivity"
            android:label="@string/addbutton_activity_label"
            android:launchMode="singleTask"
            android:parentActivityName=".feature.home.HomeActivity"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="audio/*"/>
            </intent-filter>
        </activity>

    </application>

</manifest>